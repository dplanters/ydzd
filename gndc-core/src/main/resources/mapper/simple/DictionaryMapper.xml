<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gndc.core.mapper.simple.DictionaryMapper">

    <select id="selectByDict" parameterType="com.gndc.core.etc.dictionary.DictionaryExt"
            resultMap="BaseResultMapEx">
        select
        <include refid="Base_Column_List"/>
        from dc_dict
        where 1=1
        <if test="optionGroup != null ">
            and option_group = #{optionGroup}
        </if>
        <if test="status != 0 and status != null">
            and status = #{status}
        </if>
        order by id
    </select>
</mapper>